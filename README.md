# SpotTheSpot

An application written in Kotlin that uses machine learning to identify cancerous spots based on an image taken through the app. Using the colours on the spot, blurred edges, size and symmetry image processing can be used to identify potentially harmful spots.
